version: '3'
services:
        web:
                image: projectskin
                build: .
                ports:
                        - "8086:8086"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
