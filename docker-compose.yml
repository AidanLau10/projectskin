version: '3'
services:
        web:
                image: projectskin
                build: .
                ports:
                        - "8023:8023"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
