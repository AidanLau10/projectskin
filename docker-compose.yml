version: '3'
services:
        web:
                image: projectskin
                build: .
                ports:
                        - "8015:8015"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
